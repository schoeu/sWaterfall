/**
 * Created by schoeu on 2014/12/18.
 *
 * {
 *
 * }
 *
 */
/*
    $(function($){
        $.fn.extend({
            sWaterfall : function(options){
                var contanin = this;
                var columnWidth,minColumnCount,resNum;
                if(options){
                    columnWidth = options.columnWidth;
                    minColumnCount = options.minColumnCount;
                    resColNum = Math.min(Math.ceil(contanin.width()/columnWidth),minColumnCount) || 2;
                }
            }
        });
    })(jQuery);
*/
